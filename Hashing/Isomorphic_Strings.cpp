#include<bits/stdc++.h>
using namespace std;

int main(){

    string str="aab";
    string s="xxy";

    map<char, char> ans;
    for(int i=0; i<str.length(); i++){
        if(ans.find(str[i])!=ans.end()){
            
        }
    }

    return 0;
}